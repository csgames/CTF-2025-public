# Blazingly Fast

Write `~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~` in chat. Top 2 blocks will be overwritten to a flag and can be collected.

----

Écrire `~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~` dans le chat. Cela va overflow les 2 caractères dans la 2 premières cases de la map, et peut être collecté.